# name: cairo ci

# on:
#   push:
#     branches: [ "master" ]
#   pull_request:
#     branches: [ "master" ]

# env:
#   CARGO_TERM_COLOR: always

# jobs:
#   build:

#     runs-on: ubuntu-latest

#     steps:
#     - uses: actions/checkout@v3
#     - uses: dtolnay/rust-toolchain@stable
    
#     - name: rust version
#       run: |
#         rustup override set stable && rustup update
#         rustup --version
#     - name : test app
#       run: |
#         cargo run --bin cairo-run -- --single-file /cairo-app/src/lib.cairo
        
 


